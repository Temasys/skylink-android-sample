<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sg.com.temasys.skylink.sdk.sample">

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true"></uses-feature>

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.CAMERA"></uses-permission>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/vls_app"
        android:allowBackup="false">

        <activity
            android:name="sg.com.temasys.skylink.sdk.sample.JoinRoomActivity"
            android:label="@string/app_name"
            android:screenOrientation="sensor"
            android:configChanges="orientation|screenSize"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https"
                    android:host="apprtc.appspot.com" />
                <data
                    android:scheme="http"
                    android:host="apprtc.appspot.com" />
            </intent-filter>
        </activity>

        <activity
            android:name="sg.com.temasys.skylink.sdk.sample.RoomViewActivity"
            android:label="@string/app_name"
            android:screenOrientation="sensor"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="sg.com.temasys.skylink.sdk.sample.JoinRoomActivity" />

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="apprtc.appspot.com"
                    android:scheme="https" />
                <data
                    android:host="apprtc.appspot.com"
                    android:scheme="http" />
            </intent-filter>
        </activity>

        <!-- This is necessary for file browser -->
        <activity
            android:name="sg.com.temasys.skylink.sdk.sample.FileBrowserActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="File browser">
            <intent-filter>
                <action android:name="ua.com.vassiliev.androidfilebrowser.SELECT_DIRECTORY_ACTION"></action>
            </intent-filter>
        </activity>
        <!-- End of file browser necessity -->

        <activity
            android:name="com.warting.bubbles.HelloBubblesActivity"
            android:label="@string/app_name">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

    </application>

</manifest>
